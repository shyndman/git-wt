# git-wt

A small Zsh command for Git worktrees.

```zsh
./install.zsh
git wt init
git wt add NAME
git wt rm NAME
git wt completions
```

Worktrees live in `.worktrees/`. New worktrees run `.wtinit`.
